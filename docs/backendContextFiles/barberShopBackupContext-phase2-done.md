# barberShopBackupContext.md

> این فایل، مرجع کامل وضعیت پروژه‌ست. هر چت جدیدی که با Claude درباره‌ی این
> پروژه باز می‌شه، باید این فایل رو بخونه تا بفهمه داستان از چه قراره.
> هر مرحله از نقشه‌راه فقط وقتی تیک می‌خوره که کاربر (صاحب پروژه) تست کرده و
> تایید کرده که کار می‌کنه. تا اون موقع `[ ]` می‌مونه.

آخرین به‌روزرسانی: فاز ۲ تمام شد ✅ — با اسکریپت `server/test-phase2.mjs` (Node، فچ خودکار) کل مسیر auth (register/login/me) + CRUD کامل services + CRUD کامل barbers (شامل permissions) تست شد: ۱۲ از ۱۲ تست موفق. آماده‌ی شروع فاز ۳ (اتصال فرانت به بک‌اند).

---

## ⚠️ نکته‌ی حیاتی درباره‌ی سبک همکاری

**صاحب پروژه قصد یادگیری کدنویسی نداره.** هدف صرفاً راه‌اندازی و تحویل
پروژه‌ی کارکرده‌ست، نه آموزش. یعنی تو هر چت بعدی:
- Claude باید کارها رو مستقیماً خودش انجام بده (نوشتن فایل‌ها، اجرای
  دستورات لازم در sandbox، دادن دستورات آماده‌ی کپی‌پیست برای چیزهایی که
  فقط روی سیستم خودِ کاربر قابل اجراست مثل `npm install` یا ست‌کردن دیتابیس).
- به‌جای توضیح مفهومی طولانی یا آموزش «چرا این‌جوری»، باید نتیجه/فایل
  نهایی رو تحویل بده و فقط در حد لازم خلاصه توضیح بده.
- سوال بپرس فقط وقتی تصمیمی معماری/محصولی لازمه که Claude نمی‌تونه خودش
  حدس بزنه؛ در غیر این صورت با بهترین‌ practice پیش بره و ادامه بده.

---

## ۱. معرفی کلی پروژه

سایت رزرو نوبت یک **آرایشگاه مردانه**، کاملاً **فارسی** (RTL)، با نسخه‌ی
**PWA** (قابل نصب روی گوشی). فرانت تقریباً تکمیله. بک‌اند از صفر داریم
می‌سازیم و قراره جایگزین لایه‌ی mock فعلی (`lib/data/*.ts`) بشه.

## ۲. استک فنی

| بخش | تکنولوژی |
|---|---|
| فرانت | Next.js 14 (App Router)، TypeScript، Tailwind، shadcn/radix-ui، react-hook-form + zod، react-multi-date-picker (تقویم جلالی) |
| بک‌اند | Node.js + **Express** + **Prisma ORM** (TypeScript) |
| دیتابیس | **PostgreSQL** |
| احراز هویت | JWT (Bearer token)، bcrypt برای هش پسورد |
| نوتیفیکیشن | **Web Push API** استاندارد (پکیج `web-push`, رایگان، بدون سرویس ثالث) + جدول Notification برای inbox داخل پنل |
| محل بک‌اند | پوشه‌ی `server/` **داخل همین ریپو** (نه ریپوی جدا)، پکیج جدا از فرانت |

### چرا این تصمیم‌ها؟
- Express + Prisma: ساده‌تر از NestJS برای یادگیری و شروع سریع، مستندات و جامعه‌ی بزرگ.
- `server/` داخل همون ریپو: مدیریت راحت‌تر برای توسعه‌ی تک‌نفره، در آینده به‌راحتی قابل جداشدنه.
- Web Push به‌جای Firebase/OneSignal: کاملاً رایگانه و چون PWA از قبل service worker داره، اضافه‌کردنش هزینه‌ی فنی کمی داره.

⚠️ نکته: یک‌بار تو چت «با nest نوشته شده» گفته شد. **فرض شده منظور Next.js
بوده** (نه فریم‌ورک NestJS) چون کل ساختار آپلودشده Next.js App Router است.
اگه اشتباهه، باید بک‌اند از Express به NestJS تغییر کنه — **این هنوز به‌صورت
صریح توسط کاربر تایید نشده، باید مرحله اول تایید بشه.**

## ۳. ساختار فرانت (خلاصه)

```
app/
  (site)/            صفحه اصلی، booking، login، register  — عمومی
  admin/             پنل ادمین: dashboard, barbers, services, bookings, holidays, reports, settings
  barber/            پنل آرایشگر: dashboard, bookings, customers, schedule, services, time-off
  customer/          پنل مشتری: dashboard, bookings, history, profile
components/          UI کامپوننت‌ها (shadcn-style) + نوبار/سایدبار مخصوص هر نقش
lib/data/            *** لایه‌ی mock فعلی که باید با API واقعی جایگزین بشه ***
  services.ts, barbers.ts, bookings.ts, appointments.ts, time-off.ts,
  holidays.ts, salon-settings.ts, mock-session.ts, mock-accounts.ts,
  availability.ts, barber-permissions.ts, admin-session.ts,
  barber-session.ts, customer-session.ts
```

`mock-session.ts` سشن رو تو `localStorage` شبیه‌سازی می‌کنه — این باید با
یک AuthContext واقعی (JWT از بک‌اند) جایگزین بشه.

### استراکچر کامل `app/` (خروجی `tree app /f`)

```
app/
│   globals.css
│   layout.tsx
│
├───(site)                      ← عمومی، بدون نیاز به لاگین
│   │   layout.tsx
│   │   page.tsx                ← صفحه اصلی
│   │
│   ├───booking
│   │       page.tsx            ← فرم رزرو نوبت (دو مسیر: آرایشگر/سرویس)
│   │
│   ├───login
│   │       page.tsx
│   │
│   └───register
│           page.tsx            ← فقط رول CUSTOMER می‌سازه
│
├───admin
│   │   layout.tsx
│   │
│   ├───barbers
│   │       page.tsx            ← افزودن/ویرایش آرایشگر + پرمیشن‌ها
│   │
│   ├───bookings
│   │       page.tsx            ← لیست کل نوبت‌های سالن
│   │
│   ├───dashboard
│   │       page.tsx
│   │
│   ├───holidays
│   │       page.tsx            ← تعطیلات سالن + مرخصی آرایشگرها
│   │
│   ├───reports
│   │       page.tsx            ← گزارش‌ها (بر اساس آرایشگر/وضعیت)
│   │
│   ├───services
│   │       page.tsx            ← CRUD خدمات + قیمت پیش‌فرض سالن
│   │
│   └───settings
│           page.tsx            ← اطلاعات سالن، ساعات کاری، تغییر رمز ادمین
│
├───barber
│   │   layout.tsx
│   │   page.tsx
│   │
│   ├───bookings
│   │       page.tsx            ← نوبت‌های امروز + شروع/پایان سرویس
│   │
│   ├───customers
│   │       page.tsx            ← فقط مشتری‌های خودش
│   │
│   ├───dashboard
│   │       page.tsx
│   │
│   ├───schedule
│   │       page.tsx            ← فقط اگه manageSchedule داشته باشه
│   │
│   ├───services
│   │       page.tsx            ← فقط اگه manageServices داشته باشه
│   │
│   └───time-off
│           page.tsx            ← ثبت مستقیم (با پرمیشن) یا درخواست (بدون پرمیشن)
│
└───customer
    │   layout.tsx
    │
    ├───bookings
    │       page.tsx            ← نوبت‌های فعلی/آینده
    │
    ├───dashboard
    │       page.tsx
    │
    ├───history
    │       page.tsx            ← نوبت‌های گذشته + امکان ثبت امتیاز/نظر
    │
    └───profile
            page.tsx            ← ویرایش اطلاعات + تغییر رمز (چون خودش ثبت‌نام کرده)
```

## ۴. ساختار بک‌اند (اسکلت اولیه‌ای که ساخته شده)

> ✅ وضعیت فعلی: این ساختار روی سیستم کاربر `npm install` شده، migration
> اولیه (`20260916134915_init`) اجرا و روی دیتابیس اعمال شده، seed با
> موفقیت اجرا شده، و سرور با `npm run dev` بالا اومده و `/health` تست
> و تایید شده. فاز ۱ نقشه‌راه بسته شد.
>
> ⚠️ نکته‌ی ساختاری برای چت‌های بعدی: مسیر واقعی پروژه روی سیستم کاربر
> `D:\workarea\barBerShop\barbershop\server\` هست (نه یه پوشه‌ی `server`
> تودرتوی اضافه — این مشکل قبلاً پیش اومده بود و برطرف شد، حواس باشه
> دوباره پیشنهاد نشه). `.env` و `docker-compose.yml` روی سیستم کاربر
> از قبل ساخته و تنظیم شدن (کلیدهای `.env`: `DATABASE_URL`, `PORT`,
> `CLIENT_ORIGIN`, `JWT_SECRET`, `JWT_EXPIRES_IN` — دقیقاً مطابق
> `.env.example` پروژه). نسخه‌ی Prisma **باید همون `5.22.0`** بمونه؛
> پیشنهاد آپدیت به `8.x` که خودِ CLI نشون می‌ده باید نادیده گرفته بشه
> چون schema/کد با v5 نوشته شده.

```
server/
  prisma/
    schema.prisma     مدل‌های User, BarberProfile, Service, BarberService,
                       Booking, TimeOff, SalonHoliday, SalonSettings, WorkingHours
    seed.ts            داده‌ی اولیه (همون اکانت‌های mock-accounts.ts)
  src/
    config/env.ts
    lib/prisma.ts
    middleware/        auth.ts (requireAuth/requireRole), errorHandler.ts
    utils/             jwt.ts, password.ts, asyncHandler.ts, AppError.ts
    modules/
      auth/            login, register, me   ✅ پیاده‌سازی شده
      services/        CRUD خدمات             ✅ پیاده‌سازی شده
      barbers/         CRUD آرایشگر + permissions   ✅ پیاده‌سازی شده
      bookings/        (فاز بعدی)
    routes/index.ts
    app.ts / server.ts
  package.json, tsconfig.json, .env.example, README.md, docker-compose.yml
```

> پورت‌ها عمداً غیرپیش‌فرض‌اند تا با پروژه‌ی دیگه‌ی کاربر (که Docker/دیتابیس
> خودش رو داره) تداخل نکنن: Postgres روی هاست `5433`، بک‌اند Express روی
> `4010`. Postgres این پروژه هم داخل Docker بالا میاد (`docker compose up -d`
> داخل `server/`) با container/volume اختصاصی (`barbershop_postgres` /
> `barbershop_pgdata`).

اکانت‌های seed (مطابق mock-accounts.ts فرانت):

| نقش | موبایل | پسورد |
|---|---|---|
| ادمین | 09120000001 | admin123 |
| آرایشگر | 09120000002 | barber123 |
| مشتری | 09120000003 | customer123 |

## ۵. نقش‌ها و سطح دسترسی

### قانون ثبت‌نام و مدیریت نقش‌ها (مهم)
- **ثبت‌نام عمومی (فرم register) همیشه و فقط نقش `CUSTOMER` می‌سازه.**
  هیچ راهی برای اینکه یک کاربر با ثبت‌نام خودش آرایشگر/مدیر/ادمین بشه وجود نداره.
- نقش‌های `BARBER`, `MANAGER`, `ADMIN` **فقط توسط ادمین** ساخته می‌شن
  (از پنل ادمین، مثل همون `createBarber` که در `barbers.ts` هست).
- برای این نقش‌ها (`BARBER`, `MANAGER`)، **موبایل (یوزرنیم) و پسورد رو
  خودِ ادمین موقع ساخت تعیین می‌کنه**، و خودِ اون کاربر (آرایشگر/مدیر سالن)
  **اجازه‌ی تغییر یوزرنیم یا پسورد خودش رو نداره** — فقط ادمین می‌تونه
  این اطلاعات رو ویرایش/ریست کنه. یعنی نباید هیچ endpoint
  «تغییر پسورد خودم» برای نقش آرایشگر/مدیر سالن وجود داشته باشه؛ فقط
  ادمین (endpoint `updateBarber`) می‌تونه mobile/password رو عوض کنه.
  (این قانون شامل خودِ ادمین نمی‌شه — ادمین می‌تونه پسورد خودش رو عوض کنه،
  چون صفحه‌ی admin/settings همین قابلیت رو داره.)

### مشتری (Customer)
- **تنها رولی که از مسیر عمومی `register` ساخته می‌شه.** هر ثبت‌نامی، فارغ
  از هر چیزی، رول `CUSTOMER` می‌گیره. هیچ فرم ثبت‌نام دیگه‌ای برای رول‌های
  دیگه وجود نداره/نباید وجود داشته باشه.
- مشتری آزاده رمز عبور خودش رو تغییر بده (چون خودش با اون ثبت‌نام کرده).
- دیدن صفحه اصلی، رزرو نوبت با دو مسیر ورودی:
  1. **اول آرایشگر** → دیدن خدماتی که همون آرایشگر انجام می‌ده → انتخاب سرویس
  2. **اول سرویس** → دیدن آرایشگرهایی که اون سرویس رو انجام می‌دن → انتخاب آرایشگر
  - بعد: انتخاب تاریخ (جلالی) + ساعت + توضیحات → ثبت نهایی
- صفحه‌ی شخصی: دیدن/ویرایش پروفایل، نوبت‌های فعلی و تاریخچه
- بعد از اتمام سرویس: می‌تونه امتیاز (۱ تا ۵) + نظر برای آرایشگر ثبت کنه.
  **امتیازها عمومی هستن** — همه (حتی قبل از رزرو) می‌تونن ببینن.

### آرایشگر (Barber)
- **نمی‌تونه خودش با رول آرایشگر ثبت‌نام کنه.** حساب آرایشگر فقط توسط ادمین
  از پنل ساخته می‌شه (`admin/barbers` → افزودن آرایشگر) و ادمین همون لحظه
  موبایل (username) و پسورد اولیه رو تعیین می‌کنه.
- آرایشگر **حق تغییر موبایل یا پسورد خودش رو نداره** — این‌ها فقط از طریق
  ادمین قابل تغییرن. تو صفحه‌ی پروفایل/تنظیمات آرایشگر نباید فرم تغییر
  رمز/موبایل وجود داشته باشه.
- پنل شخصی خودش، فقط نوبت‌ها و مشتری‌های **خودش** رو می‌بینه (هیچ‌وقت
  مشتری‌ها/نوبت‌های آرایشگر دیگه رو نمی‌بینه).
- پرمیشن‌های اختیاری (هرکدوم رو ادمین جدا فعال/غیرفعال می‌کنه، هیچ پیش‌فرض
  خودکاری بر اساس «نوع آرایشگر» وجود نداره):
  - `manageServices` — تعریف/ویرایش/حذف نوع خدماتی که ارائه می‌ده
  - `managePricing` — تعیین قیمت خودش (وگرنه از قیمت پیش‌فرض سالن استفاده می‌کنه)
  - `manageSchedule` — تعیین ساعات کاری خودش
  - `manageTimeOff` — ثبت مستقیم مرخصی بدون تایید ادمین
  - `blockSlots` — بستن دستی یک اسلات زمانی خاص
  - `cancelOwnBookings` — لغو نوبت‌های تاییدشده‌ی خودش
- پرمیشن‌های ثابت (به همه آرایشگرها خودکار تعلق می‌گیره): دیدن داشبورد خودش،
  نوبت‌های خودش، مشتری‌های خودش، تاریخچه‌ی خودش، شروع/پایان سرویس.
- **شروع/پایان کار**: روی خودِ `Booking` انجام می‌شه —
  `CONFIRMED → IN_PROGRESS → COMPLETED` (نه یه مفهوم جدا مثل «شیفت روزانه»). ✅ قطعی

### مدیر سالن (Manager) — پرمیشن قطعی شد ✅
- تصمیم شد: **زیرمجموعه‌ی دسترسی ادمین، یک سالن (نه چندسالنی)**.
- مثل آرایشگر، **فقط ادمین** حساب مدیر سالن رو می‌سازه و username/password
  رو تعیین می‌کنه؛ مدیر سالن هم حق تغییر خودشون رو نداره.
- دسترسی‌هاش: دیدن همه‌ی نوبت‌ها، گزارش‌ها، لیست آرایشگرها + تایید/رد
  درخواست مرخصی (`LeaveRequest`).
- **ندارد**: ساخت/حذف آرایشگر، تغییر پرمیشن‌های آرایشگر، دسترسی به
  تنظیمات کلی سالن (قیمت پیش‌فرض، ساعات کاری کلی، اطلاعات تماس سالن).

### ادمین (Admin)
- بالاترین سطح دسترسی، به همه‌چیز.
- **تنها نقشی که مجاز به ساختن حساب برای بقیه‌ی رول‌ها (آرایشگر، مدیر
  سالن، حتی ادمین دیگه) هست** — و در همون لحظه‌ی ساخت، username/password
  رو خودش تعیین می‌کنه.
- اضافه‌کردن آرایشگر + تعیین دقیق پرمیشن‌های هرکدوم.
- تایید/رد درخواست مرخصیِ آرایشگرهایی که پرمیشن `manageTimeOff` رو ندارن —
  از طریق مدل `LeaveRequest` (pending/approved/rejected)، جدای جدول نهایی
  `TimeOff`. ✅ قطعی — مدیر سالن هم می‌تونه همین کار رو انجام بده.
- تعیین قیمت پیش‌فرض سالن (آرایشگرهای بدون `managePricing` از همین استفاده می‌کنن).
- تعیین روزهای تعطیلی سالن.

## ۶. جریان‌های کلیدی کسب‌وکار

1. **رزرو نوبت** — دو مسیر ورودی (بالا توضیح داده شد) → یک `Booking`
   با status اولیه‌ی `CONFIRMED` ساخته می‌شه.
2. **شروع/پایان سرویس** — آرایشگر روز رزرو، دکمه‌ی «شروع کار» می‌زنه
   (`IN_PROGRESS`)، بعد «پایان کار» (`COMPLETED`).
3. **امتیازدهی** — بعد از `COMPLETED`، مشتری امتیاز ۱ تا ۵ + نظر ثبت می‌کنه.
   معدل امتیاز + تعداد آرا رو تو پروفایل عمومی هر آرایشگر نشون می‌دیم.
4. **مرخصی** — آرایشگر بدون پرمیشن → درخواست می‌ده → صف انتظار ادمین →
   تایید/رد. آرایشگر با پرمیشن → مستقیم ثبت می‌شه، نیازی به تایید نیست.
5. **نوتیفیکیشن** — رویدادهایی که باید Push/inbox بفرستن (فهرست اولیه،
   قابل اضافه‌شدن):
   - تایید ثبت نوبت برای مشتری
   - یادآوری نزدیک‌شدن نوبت
   - شروع/پایان کار (به مشتری اطلاع بده کارش شروع/تموم شد)
   - تایید یا رد درخواست مرخصی (به آرایشگر)
   - درخواست مرخصی جدید (به ادمین)
   - نوبت جدید ثبت شد (به آرایشگر مربوطه)
   - لغو نوبت (به هردو طرف)

## ۷. تصمیم‌ها و توافق‌های ثبت‌شده در این چت (Policy Log)

| # | موضوع | تصمیم |
|---|---|---|
| 1 | بک‌اند | Node.js + Express + Prisma، داخل `server/` همین ریپو |
| 2 | دیتابیس | PostgreSQL |
| 3 | نقش «مدیر سالن» | زیرمجموعه‌ی دسترسی ادمین، یک سالن (نه چندسالنی) |
| 4 | نوتیفیکیشن | رایگان + باید حتی با برنامه‌ی بسته هم برسه → Web Push API |
| 5 | امتیازدهی | عمومی، همه (حتی قبل از رزرو) می‌بینن |
| 6 | ثبت‌نام عمومی | هر کسی از فرم register ثبت‌نام کنه، **همیشه با رول `CUSTOMER`** ساخته می‌شه. هیچ مسیر عمومی‌ای برای ساخته‌شدن با رول آرایشگر/ادمین/مدیر وجود نداره. |
| 7 | ساخت رول‌های غیرمشتری | آرایشگر، ادمین، مدیر سالن — **فقط توسط ادمین** از پنل ساخته می‌شن. ادمین خودش username (موبایل) و password اولیه رو تعیین می‌کنه. |
| 8 | تغییر رمز/یوزرنیم رول‌های غیرمشتری | آرایشگر (و مدیر سالن) **اجازه‌ی تغییر موبایل/پسورد خودشون رو ندارن** — این مقادیر فقط توسط ادمین قابل تغییره. یعنی تو صفحه‌ی پروفایل آرایشگر نباید فیلد تغییر رمز/موبایل باشه. |
| 9 | هدف کاربر از این پروژه | فقط **راه‌اندازی و تحویل کارکردن پروژه**، نه یادگیری برنامه‌نویسی. یعنی Claude باید مستقیم کار رو انجام بده، توضیح آموزشی اضافه ندونه مگر خواسته بشه، و مسئولیت تصمیم‌های فنی جزئی رو خودش بگیره تا کار پیش بره. |
| 10 | ایزوله‌سازی از پروژه‌ی دیگه‌ی روی سیستم | کاربر یه پروژه‌ی دیگه با Docker+دیتابیس رو سیستمش داره؛ نباید تداخل کنن. تصمیم: Postgres این پروژه هم تو Docker (`docker-compose.yml` داخل `server/`)، با نام container/volume اختصاصی (`barbershop_postgres` / `barbershop_pgdata`) و پورت‌های غیرپیش‌فرض: Postgres روی هاست `5433` (نه 5432)، بک‌اند Express روی `4010` (نه 4000). اگه این پورت‌ها هم تداخل داشتن باید عوض بشن. |

## ۸. فرضیات — همه با پاسخ صریح کاربر تایید و قطعی شدن (فاز ۰ بسته شد ✅)

- [x] «nest» = Next.js بوده؛ بک‌اند همون **Express** می‌مونه
- [x] شروع/پایان کار = فقط تغییر status روی همون `Booking` (نه entity جدا)
- [x] مرخصیِ بدون‌پرمیشن → نیاز به مدل جدا `LeaveRequest`
      (pending/approved/rejected) و تایید/رد دستی ادمین (یا مدیر سالن)
- [x] پرمیشن «مدیر سالن» **قطعی شد**: دیدن همه‌ی نوبت‌ها + گزارش‌ها +
      لیست آرایشگرها، و تایید/رد درخواست مرخصی. **بدون** اجازه‌ی
      ساخت/حذف آرایشگر، تغییر پرمیشن آرایشگرها، یا دسترسی به تنظیمات کلی
      سالن (قیمت پیش‌فرض، ساعات کاری کلی، اطلاعات سالن).

---

## ۹. نقشه‌راه اجرا (اولویت‌بندی‌شده)

> قانون: هر آیتم فقط بعد از اینکه کاربر تست کرد و در چت گفت «تایید شد»،
> از `[ ]` به `[x]` تغییر می‌کنه و این فایل به‌روز می‌شه.

### فاز ۰ — تایید فرضیات باز ✅ تمام
- [x] تایید ۴ فرض بخش ۸ (همگی صریحاً توسط کاربر تایید شدن)

### فاز ۱ — راه‌اندازی پایه بک‌اند ✅ تمام
- [x] نصب دیپندنسی‌ها (`npm install` داخل `server/`)
- [x] ساخت دیتابیس Postgres + تنظیم `.env`
- [x] اجرای اولین migration (`prisma migrate dev`)
- [x] اجرای seed و بررسی داده‌ها
- [x] بالا آوردن سرور (`npm run dev`) و تست `/health`

### فاز ۲ — تست ماژول‌های آماده‌شده ✅ تمام
- [x] تست کامل auth (login/register/me)
- [x] تست CRUD services
- [x] تست CRUD barbers + تنظیم permissions

> ✅ روش تست: اسکریپت `server/test-phase2.mjs` (Node 18+، fetch توکار، بدون
> نیاز به Postman/jq). هر بار با `npm run dev` (ترمینال ۱) +
> `docker compose up -d` (Postgres) سرور رو بالا نگه دار، بعد تو ترمینال
> دیگه `node test-phase2.mjs` رو اجرا کن. نتیجه: ۱۲ از ۱۲ تست پاس (register،
> login admin، me، GET/POST/PATCH/DELETE services، GET/POST/PATCH/DELETE
> barbers، PATCH permissions).
>
> نکات تاییدشده درباره‌ی API که برای فازهای بعد لازمه:
> - همه‌ی routeها زیر پیشوند `/api` mount شدن: `http://localhost:4010/api/...`
> - پاسخ `POST /auth/login` شامل فیلد `token` هست (نه `accessToken`).
> - `POST /barbers` بادی‌ای شبیه `{ name, mobile, password, bio, initials, serviceIds }` قبول می‌کنه.
> - `POST /services` بادی‌ای شبیه `{ title, desc, priceValue, icon, featured }` قبول می‌کنه.
> - این اسکریپت تست رو نگه دار (حذفش نکن)؛ تو فازهای بعدی (بوکینگ و...) می‌شه
>   روش گسترشش داد.

### فاز ۳ — اتصال فرانت به بک‌اند (پایه)
- [ ] ساخت `lib/api.ts` (fetch wrapper + مدیریت توکن)
- [ ] ساخت AuthContext واقعی، جایگزینی `mock-session.ts`
- [ ] وصل‌کردن صفحه‌ی login/register به API واقعی
- [ ] وصل‌کردن صفحه‌ی admin/services و admin/barbers به API واقعی

### فاز ۴ — Bookings (هسته‌ی اصلی سیستم)
- [ ] طراحی و پیاده‌سازی ماژول `bookings` (یکی‌سازی booking+appointment)
- [ ] منطق واقعی availability (جایگزین هش تصادفی فعلی) — بر اساس
      ساعات کاری + تعطیلات + مرخصی + نوبت‌های موجود
- [ ] وصل‌کردن صفحه‌ی بوکینگ مشتری (هر دو مسیر ورودی) به API واقعی
- [ ] وصل‌کردن پنل آرایشگر (نوبت‌های امروز، مشتریان من) به API واقعی
- [ ] وصل‌کردن پنل مشتری (نوبت‌های من، تاریخچه) به API واقعی
- [ ] وصل‌کردن admin/bookings به API واقعی

### فاز ۵ — مرخصی، تعطیلات، تنظیمات سالن
- [ ] ماژول `LeaveRequest` (درخواست/تایید/رد) برای آرایشگرهای بدون پرمیشن
- [ ] ماژول `time-off` مستقیم برای آرایشگرهای دارای پرمیشن
- [ ] ماژول `holidays` (تعطیلات سالن)
- [ ] ماژول `settings` (SalonSettings + WorkingHours)
- [ ] وصل‌کردن صفحات مربوطه تو admin و barber پنل

### فاز ۶ — شروع/پایان کار و امتیازدهی
- [ ] endpoint شروع/پایان سرویس (تغییر status روی Booking)
- [ ] مدل و endpoint امتیاز/نظر (Rating/Review)
- [ ] محاسبه و نمایش معدل امتیاز + تعداد آرا در پروفایل عمومی آرایشگر
- [ ] وصل‌کردن UI مربوطه (دکمه شروع/پایان در پنل آرایشگر، فرم امتیازدهی در پنل مشتری)

### فاز ۷ — نوتیفیکیشن
- [ ] مدل `Notification` (inbox داخل پنل)
- [ ] راه‌اندازی Web Push (تولید VAPID keys، پکیج `web-push`)
- [ ] ذخیره‌ی subscription کاربر (از service worker فرانت)
- [ ] اتصال رویدادهای فاز ۴ تا ۶ به سیستم نوتیفیکیشن (فهرست بخش ۶)
- [ ] UI نمایش inbox نوتیف تو هر پنل

### فاز ۸ — گزارش‌ها و پنل مدیر سالن
- [ ] ماژول `reports` (aggregate روی Booking برای admin/reports)
- [ ] تعریف نهایی پرمیشن نقش Manager + پیاده‌سازی

### فاز ۹ — سخت‌سازی و آماده‌سازی دیپلوی
- [ ] rate limiting، helmet، بازبینی امنیتی کلی
- [ ] بررسی نهایی validation همه‌ی endpointها
- [ ] تنظیمات production (env، migrate deploy، لاگ‌گیری)
- [ ] مستندسازی نهایی API

---

## ۱۰. نکات مهم برای Claude در چت‌های بعدی

- همیشه اول همین فایل رو مرجع بگیر، نه فرض‌های خودت.
- کدی که قبلاً نوشته شده رو (بخش ۴) تکرار نکن مگر کاربر بگه تغییرش بده.
- هیچ مرحله‌ای رو «تمام‌شده» فرض نکن مگر این فایل صراحتاً `[x]` داشته باشه.
- زبان پاسخ‌ها فارسی، و سبک کدنویسی/کامنت‌ها هم فارسی (مطابق کدهای فعلی پروژه).
- **کاربر قصد یادگیری برنامه‌نویسی نداره، فقط می‌خواد پروژه راه بیفته.**
  یعنی:
  - توضیح آموزشیِ اضافه (چرا فلان الگو، تفاوت X و Y و ...) نده مگر صراحتاً
    خواسته بشه؛ مستقیم برو سر اجرا و نتیجه.
  - تصمیم‌های فنی جزئی (اسم متغیر، ساختار داخلی فایل، جزئیات پیاده‌سازی)
    رو خودت بگیر و کار رو جلو ببر؛ فقط برای تصمیم‌های سطح‌بالا (که رو
    scope/dataیا رفتار کاربر تاثیر داره) سوال بپرس.
  - همیشه، تا جایی که ممکنه، به‌جای «این کارو بکن» خودت مستقیم انجامش بده.
- ثبت‌نام عمومی همیشه رول `CUSTOMER` می‌سازه؛ رول‌های دیگه فقط با ابزار
  ادمین (که هنوز پیاده نشده — بخشی از فاز ۳/۴) ساخته می‌شن، و
  username/password اون‌ها فقط از طریق ادمین قابل تغییره (نه خودشون).
